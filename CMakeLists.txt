PROJECT( Cernia )

# LIGNE DE MERDE
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

SET( SOURCES
	  main.cxx
)

FIND_PACKAGE( Qt4 REQUIRED )

SET( QT_USE_QTGUI FALSE )

INCLUDE( ${QT_USE_FILE} )

ADD_EXECUTABLE( Cernia ${SOURCES} )

TARGET_LINK_LIBRARIES( Cernia ${QT_QTCORE_LIBRARY} )
